@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">
        Welcome MovieApp
        @if (User.Identity?.Name != null)
        {
            @User.Identity?.Name
        }
    </h1>
</div>